# Base de datos (XAMPP usa root sin contraseña por defecto)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=rca_database

# Servidor
SERVER_HOST=0.0.0.0
SERVER_PORT=8007

# Seguridad JWT (CAMBIAR en producción)
SECRET_KEY=cambiar-esta-clave-en-produccion-minimo-32-caracteres

# Rutas (ajustar según tu instalación)
ARCHIVOS_PATH=../archivos
RESPALDOS_PATH=../respaldos

